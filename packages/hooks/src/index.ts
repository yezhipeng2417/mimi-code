/**
 * @mimi/hooks — Hook system for lifecycle events.
 *
 * Depends on: @mimi/core
 */

export { HookRunner } from './runner.js';
export type {
  HookConfig,
  HookContext,
  HookExecutionResult,
} from './types.js';
